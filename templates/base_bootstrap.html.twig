<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
        <style>
            body {
                background: linear-gradient(135deg, #a4c5cc 0%, #8ab5bf 100%);
                min-height: 100vh;
                padding: 20px 0;
                position: relative;
            }

            /* Add subtle pattern overlay */
            body::before {
                content: '';
                position: fixed;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background-image: radial-gradient(rgba(255, 255, 255, 0.2) 1px, transparent 1px);
                background-size: 20px 20px;
                pointer-events: none;
            }

            .container {
                position: relative; /* Keep content above pattern */
            }

            .card {
                background-color: white;
                box-shadow: 0 4px 6px rgba(0,0,0,0.1);
                transition: all 0.3s ease;
                border: none;
                border-radius: 8px;
            }

            .card:hover {
                transform: translateY(-5px);
                box-shadow: 0 8px 15px rgba(0,0,0,0.2);
            }

            .card-img-top {
                width: 100%;
                height: 400px; /* Adjusted for better proportion */
                object-fit: cover;
                border-top-left-radius: 8px;
                border-top-right-radius: 8px;
            }

            .card-body {
                padding: 1.5rem;
            }

            .card-title {
                color: #2c3e50;
                font-weight: 600;
            }

            .btn-outline-primary {
                border-color: #8ab5bf;
                color: #8ab5bf;
                transition: all 0.3s ease;
            }

            .btn-outline-primary:hover {
                background-color: #8ab5bf;
                border-color: #8ab5bf;
                color: white;
            }

            .btn-outline-danger {
                border-color: #dc3545;
                color: #dc3545;
                transition: all 0.3s ease;
            }

            .btn-outline-danger:hover {
                background-color: #dc3545;
                color: white;
            }

            .book-card {
                position: relative;
                overflow: hidden;
            }

            .book-overlay {
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background: linear-gradient(
                    rgba(0, 0, 0, 0.9),
                    rgba(0, 0, 0, 0.7)
                );
                color: white;
                padding: 2rem;
                opacity: 0;
                transition: all 0.4s ease;
                pointer-events: none;
                display: flex;
                flex-direction: column;
                justify-content: center;
            }

            .book-card:hover .book-overlay {
                opacity: 1;
                pointer-events: auto;
            }

            .overlay-title {
                margin-bottom: 1rem;
                color: white;
                font-size: 1.5rem;
                font-weight: 600;
            }

            .overlay-description {
                font-size: 1rem;
                line-height: 1.6;
                color: rgba(255, 255, 255, 0.9);
            }

            .card-footer {
                background-color: transparent;
                border-top: 1px solid rgba(0,0,0,0.1);
                padding: 1rem 1.5rem;
            }

            /* For placeholder when no image */
            .card-img-top.bg-light {
                height: 400px;
                display: flex;
                align-items: center;
                justify-content: center;
                background-color: #f8f9fa;
            }
            .navbar {
            padding: 1rem 0;
            }

            .navbar-brand {
                color: #a4c5cc;
                font-weight: 600;
            }

            .nav-link {
                color: #4a4a4a;
                transition: color 0.3s ease;
            }

            .nav-link:hover {
                color: #a4c5cc;
            }
        </style>
    </head>
    <body>
    <nav class="navbar navbar-expand-lg bg-white shadow-sm mb-4">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_book_index') }}">SubconIT Library</a>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_book_index') }}">Books</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_book_new') }}">Add New Book</a>
                </li>
            </ul>
            
            <div class="navbar-nav">
                {% if app.user %}
                    <span class="nav-link">{{ app.user.email }}</span>
                    <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
                {% else %}
                    <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                    <a class="nav-link" href="{{ path('app_register') }}">Register</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
        <div class="container py-4">
            {% block body %}{% endblock %}
        </div>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>